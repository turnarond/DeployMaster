<Window x:Class="SimpleFTP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FTP 批量部署工具" Height="600" Width="800"
        Background="#FFF0F0F0"
        FontFamily="Microsoft YaHei, Segoe UI">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. 本地文件选择 -->
        <GroupBox Grid.Row="0" Header="本地文件" FontWeight="Bold" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="文件路径：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="txt_fileToUpload" Width="400" IsReadOnly="True" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Button x:Name="Btn_fileBrowse" Content="选择文件..." Click="Btn_fileBrowse_Click" 
                        Width="100" Height="25" VerticalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <!-- 2. FTP 设置 -->
        <GroupBox Grid.Row="1" Header="FTP 设置" FontWeight="Bold" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="用户名：" Grid.Row="0" Grid.Column="0" Margin="5"/>
                <TextBox x:Name="txt_user" Grid.Row="0" Grid.Column="1" Margin="5" VerticalContentAlignment="Center" Text="root"/>

                <TextBlock Text="密码：" Grid.Row="0" Grid.Column="2" Margin="5"/>
                <PasswordBox x:Name="txt_pass" Grid.Row="0" Grid.Column="3" Margin="5" VerticalContentAlignment="Center" Password="root"/>

                <TextBlock Text="远程路径：" Grid.Row="1" Grid.Column="0" Margin="5"/>
                <TextBox x:Name="txt_remotePath" Grid.Row="1" Grid.Column="1" Text="/apps/" Margin="5" VerticalContentAlignment="Center"/>

                <CheckBox x:Name="chk_rebootAfterDeploy" 
                  Content="部署后重启设备" 
                  Grid.Row="1" Grid.Column="2" 
                  Grid.ColumnSpan="2"
                  VerticalAlignment="Center" 
                  Margin="10,5,5,5"
                  IsChecked="False"/>
            </Grid>
        </GroupBox>

        <!-- 3. IP 列表 -->
        <GroupBox Grid.Row="2" Header="目标IP列表（每行一个）" FontWeight="Bold" Margin="0,0,0,10">
            <TextBox x:Name="txt_ipList" 
                     TextWrapping="NoWrap" 
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas" 
                     FontSize="12"
                     Height="100"
                     Text="192.168.1.122&#xA;192.168.1.123&#xA;192.168.1.124&#xA;192.168.1.126&#xA;192.168.1.127&#xA;192.168.1.128&#xA;" />
        </GroupBox>

        <!-- 4. 操作按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button x:Name="Btn_deploy" Content="开始部署" Click="Btn_deploy_Click" 
                    Background="#FF4CAF50" Foreground="White" FontWeight="Bold"
                    Width="100" Height="30" Margin="0,0,10,0"/>
            <Button x:Name="Btn_clearLog" Content="清除日志" Click="Btn_clear_Click" 
                    Background="#FF9E9E9E" Foreground="White"
                    Width="100" Height="30"/>
        </StackPanel>

        <!-- 5. 日志输出 -->
        <GroupBox Grid.Row="4" Header="日志输出" FontWeight="Bold">
            <TextBox x:Name="txt_log" 
             TextWrapping="Wrap" 
             AcceptsReturn="True" 
             IsReadOnly="True"
             VerticalScrollBarVisibility="Auto"
             FontFamily="Consolas" 
             FontSize="11"
             Background="Black" 
             Foreground="#00FF00"
             BorderBrush="#333"
             Height="200"
             VerticalAlignment="Top" />
        </GroupBox>

    </Grid>
</Window>